{
  "name": "water-distribution-platform",
  "version": "1.0.0",
  "description": "A platform connecting water distributors with consumers",
  "main": "server/index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm start",
    "build": "cd client && npm run build",
    "install-all": "npm install && cd server && npm install && cd ../client && npm install"
  },
  "keywords": ["water", "distribution", "platform", "tracking"],
  "author": "Water Distribution Team",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2",
    "date-fns": "^2.30.0"
  }
}
